import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "red",
    padding: "20%",
  },
  headings: {
    fontSize: "100px",
    backgroundColor: "blue",
    color: "orange",
  },
});

export { styles };
