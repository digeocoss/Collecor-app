import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "grey",
    padding: "15%",
  },
  headings: {
    fontSize: "25px",
    color: "White",
  },
});

export { styles };
